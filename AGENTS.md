# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Check the backlog before building a feature

`BACKLOG.md` lists every deferred item **and** the strategy bets (queue mode,
barber-as-agent cash wallet, barber-adoption tools), each with a **Trigger:**
saying when to raise it. When work lands on a trigger, say so — don't silently
build past it.
